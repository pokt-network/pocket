syntax = "proto3";
package genesis;

// TODO (team) move to utility module #163

option go_package = "github.com/pokt-network/pocket/shared/types/genesis";

message Account {
  string address = 1;
  string amount = 2;
}

enum Pool_Names {// TODO (team) move to p2p module #163
  DAO = 0;
  FeeCollector = 1;
  AppStakePool = 2;
  ValidatorStakePool = 3;
  ServiceNodeStakePool = 4;
  FishermanStakePool = 5;
}
