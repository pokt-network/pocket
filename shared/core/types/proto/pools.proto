syntax = "proto3";

package core;

option go_package = "github.com/pokt-network/pocket/shared/core/types";

enum Pools {
  POOLS_UNSPECIFIED = 0;
  POOLS_DAO = 1;
  POOLS_FEE_COLLECTOR = 2;
  POOLS_APP_STAKE = 3;
  POOLS_VALIDATOR_STAKE = 4;
  POOLS_SERVICE_NODE_STAKE = 5;
  POOLS_FISHERMAN_STAKE = 6;
}
