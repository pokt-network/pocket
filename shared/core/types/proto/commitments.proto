syntax = "proto3";

package core;

option go_package = "github.com/pokt-network/pocket/shared/core/types";

// CommitmentRoot represents the merkle root hash of a CommitmentState
// This is used in the verification of CommitmentProof objects
message CommitmentRoot {
    bytes hash = 1;
}
