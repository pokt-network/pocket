apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pocket-validator.fullname" . }}-test-connection"
  labels:
    {{- include "pocket-validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pocket-validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
