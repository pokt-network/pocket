syntax = "proto3";
package utility;

option go_package = "/utility/types/";

message Session {
  SessionHeader session_header = 1;
  bytes session_key = 2;
  repeated bytes session_nodes = 3;
  bytes fishermen = 4;
}

message SessionHeader {
  bytes app_public_key = 1;
  string chain = 2;
  int64 session_block_height = 3;
}